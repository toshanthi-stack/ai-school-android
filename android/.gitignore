# Android / Gradle
*.iml
.gradle/
.idea/
.kotlin/
build/
local.properties
captures/
.externalNativeBuild/
.cxx/

# Build outputs, but keep the curated release artifacts
*.apk
*.aab
!release/*.apk

# Release signing secrets - NEVER commit these
keystore.properties
keystore/
*.jks
*.keystore
